// Practise set of string
// Generating a user name with starting value @
// and ending value the length of the string

let userName = prompt("Enter your name: ");

let update_name = "@"+userName+userName.length;

console.log(update_name);